export interface CreateResponse {
  message: string;
  status: boolean;
  networkName: string;
}

export interface NetworkResponseStatus {
  message: string;
  status: boolean;
  networkName: string;
}
